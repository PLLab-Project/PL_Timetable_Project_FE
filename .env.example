# Backend API base URL
VITE_API_BASE_URL=https://pl-timetable-api-532874992461.asia-northeast3.run.app

# Production uses the Vercel same-origin proxy by default.
# Set this to false only when a production deployment must call the backend directly.
VITE_USE_SAME_ORIGIN_API=true
